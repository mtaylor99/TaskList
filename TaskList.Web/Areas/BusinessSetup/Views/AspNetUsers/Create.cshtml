@model AspNetUsersViewModel

@{
    Layout = "_Layout";
}

@{
    ViewData["Title"] = "Create Asp Net User";
}

<form asp-action="Create" method="post">
    <div class="TL-Main">
        <div class="TL-Header">
            <h2>Create Asp Net User</h2>
        </div>
        <div class="TL-Content">
            @Html.Partial("_AspNetUserCreateEditPartial", Model)
        </div>
        <br />
        <div class="TL-Footer">
            <button type="submit" class="btn btn-primary">Save</button>
            <a asp-action="List" class="btn btn-secondary">Cancel</a>
        </div>
    </div>
</form>

